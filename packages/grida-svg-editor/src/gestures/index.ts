// @grida/svg-editor — gestures module.
//
// Surface-scoped viewport-interaction layer. Sibling to `editor.keymap`.

export {
  Gestures,
  type GestureBinding,
  type GestureContext,
  type GestureId,
} from "./gestures";
export { DEFAULT_GESTURE_BINDINGS, applyDefaultGestures } from "./defaults";
