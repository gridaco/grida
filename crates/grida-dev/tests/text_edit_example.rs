//! Include the `text_edit` example module so its unit tests run with `cargo test`.
//! The module lives under `examples/text_edit/` and is used by the `wd_text_editor` example.

#[path = "../examples/text_edit/mod.rs"]
mod text_edit;
