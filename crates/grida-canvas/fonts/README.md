fonts embedded in the distribution

The single most light (size wise) font is Allerta-Regular.ttf (16kb)

for certain builds, this font is embedded and used as the default font. (for debugging windows)
