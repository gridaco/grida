import { NextRequest } from "next/server";

export async function POST(req: NextRequest) {
  // create new store if no store_id provided

  const store_name = "[generated]";

  // create new connection record
  return;
}
