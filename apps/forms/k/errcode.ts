/**
 * The number of responses has reached the allowed maximum.
 *
 * @param form.is_max_form_responses_in_total_enabled
 * @param form.max_form_responses_in_total
 *
 * XX(sqlsate prefix) 2(gf prefix) 1(user enabled feature error) 2(error code - max responses per form)
 */
export const XX212 = "XX212";
