import { css } from "@emotion/core";

export const resetUl = css`
  list-style: none;
  padding: 0;
  margin: 0;
`;

export const center = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
}