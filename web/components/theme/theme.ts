type Color = React.CSSProperties["color"];

export type HeaderTheme = {
  bg: Color;
  color: Color;
  menu: {
    resting: Color;
    hover: Color;
  };
  accent: Color;
};

export const theme = {};
