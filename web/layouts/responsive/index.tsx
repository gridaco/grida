import React, { useState } from 'react'
import {} from 'rebass';
import styled from '@emotion/styled';

interface ResponsiveLayoutProps {
  start: string
  end: string
  inherit: boolean
}

const ResponsiveLayout : React.FC<ResponsiveLayoutProps> = ({ start, end, inherit }) => {

  return (
    <div>
      
    </div>
  )
}

export default ResponsiveLayout
