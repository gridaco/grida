{
  "name": "@grida/public",
  "version": "0.0.0",
  "private": true,
  "description": "The canonical web-published content tree (origin tree) and its resolver/sync tooling. See README.md.",
  "type": "module",
  "sideEffects": true,
  "scripts": {
    "build": "node tools/cli.mjs build && node tools/cli.mjs sync --to ../editor/public",
    "test": "vitest run"
  },
  "devDependencies": {
    "dotcanvas": "workspace:*",
    "fflate": "^0.8.2",
    "vitest": "^4"
  }
}
