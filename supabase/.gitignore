# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# legacy jwt signing keys — no longer used by config, kept ignored so a
# pre-existing local signing_keys.json (private JWK) is never committed
signing_keys.json